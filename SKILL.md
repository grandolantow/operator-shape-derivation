---
name: operator-shape-derivation
description: 为单个算子或由 A、B 等组合得到的目标算子 C 推导输入、输出和中间张量 shape；结合模型与运行配置、源码及可选 profiling，交付推导说明、结构化 shape、可重跑脚本、benchmark workload 和输入生成器。用于算子开发与性能测试的用例准备，默认适配 npu-kernelbench；不默认实现 kernel、definition/reference 或采集 profiling。
---

# 算子 shape 推导与 workload 生成

将用户指定的目标及其证据转化为可复算、可追溯的测试用例。既处理已有算子，也处理“给定 A、B，生成 C = A + B”的组合需求；这里的加号不能自行解释为逐元素加法或固定串联。

## 默认工作方式

- 先扫描用户指定的配置、源码、接口材料、已有结果和 profiling，自行提取信息，再问影响结果的缺项。沿用本轮及此前已经确认的选择，不重复做完整问卷。
- 组合目标：分析调用链，提出 C 的外部输入、输出、状态及中间张量关系。已有证据足够时继续；存在影响 shape 的连接方式、分支、广播、布局或状态语义歧义时，集中询问后再完成依赖该答案的推导。
- 缺少 profiling 时，先向用户索取目标算子及对应配置的已有采集数据。用户明确没有，或明确要求按假设继续后，才生成带假设的代表场景。未回复不代表没有；期间继续固定维度、符号关系和约束分析。若此前已说明没有，不再询问。
- 用户已经明确给定的具体测试参数/用例不属于自行假设，可同步推导并导出满足约束的用例，标记为用户配置来源；仍优先索取未提供的 profiling 以核对覆盖和频率。没有回复时不额外猜测场景，也不把这些用例称为真实分布。用户明确要求纯静态分析或跳过 profiling 时遵循该范围。
- 默认 benchmark 为 npu-kernelbench。用户指定其他框架时，读取它的实际 schema 和输入构造接口后适配。默认交付到 workload 和必要的输入生成器；缺少 definition/reference 时列出接入缺口，不自动实现它们。
- 按覆盖需要选择精简用例，不设置普遍适用的固定数量。用户给定数量、范围或测试预算时优先遵循；覆盖与预算冲突时说明取舍。
- 复用目标结果的现有统一目录，保留来源和稳定 case 标识。修改配置或补充 profiling 时更新该入口，不生成并行维护的多份结果。

## 推导流程

### 1. 建立目标与材料范围

确认目标算子或组合 C、模型、后端/设备、执行阶段、最终运行配置、源码版本及输出目录。材料中已有答案直接采用；允许先保留符号，不要求用户提供一张完整参数表。

分辨最终配置、示例启动脚本和观测运行条件。记录代码 revision、相关未提交差异、配置及关键证据的定位与哈希。远端材料按环境记录和只读预检流程读取；本技能不扩大连接、路径或采集权限。

若已有材料可见性/洁净室约束，遵循所选材料范围；不要自动套用另一个 intake 技能的全套审批流程。

在代入 shape 公式前，只整理影响目标的执行量：语义范围、生效值、证据与假设依赖。按 [执行量与人工判决](references/evidence-and-shapes.md#执行量与人工判决) 检查；不能把全局请求数自动均分为每 DP batch。发现会改变结果的参数口径歧义时立即说明风险和具体待判问题，暂停受影响的推导/导出，继续独立工作；已有明确选择不重复询问。

### 2. 形成接口和 shape 关系

按本次需要沿调用者、张量变换、注册/binding、meta/规格、kernel 参数检查核对形状。新组合先建立数据流与对外接口，不需要先实现 C。

阅读 [证据与 shape 推导](references/evidence-and-shapes.md)，据此区分模型常量、执行参数、分配容量、有效工作量，以及逻辑张量与物理表示。明确每个张量的 shape、dtype、layout/stride、动态 metadata、状态/别名关系及适用约束。

需要参考组合推导方法或 Compressor 中的易错边界时，阅读 [推导案例](references/examples.md)。案例中的公式、设备限制和运行配置均不作为其他目标的默认值。

### 3. 合并 profiling 并选择用例

有 profiling 时，匹配目标实例、阶段、模型层、rank/step 和数据流。可用已安装的 profiling 分析技能处理已有采集；不可用时直接解析所需字段，或列明缺项，不以安装其他技能为强制前提。

区分真实观测、配置推导和明确假设。缺失 shape 的 kernel 名称/耗时记录不能证明完整输入形状；有 profiling 但缺少关键字段时先请求补充，继续不依赖缺项的工作。若只能构造场景，先告知证据局限并按上面的假设规则处理。

性能用例保留观测分布及其采样范围；边界用例记录覆盖理由，不混入真实频率。精简等价用例时还要比较 dtype、stride、分支、metadata 和状态访问，不能只按维度去重。

### 4. 导出、验证和交付

阅读 [workload 交付](references/workload-delivery.md)，生成目标专用的可重跑推导/导出脚本、结构化结果、workload 及必要的输入生成器。默认标准库完成 shape 算术和导出；张量构造按目标框架现有依赖实现，不因此自动安装软件。

使用目标框架的真实 schema 校验，验证用例与推导来源的对应关系。分别验证“规则选择的外部证据”和“实现是否满足预期关系”；参数变化检查覆盖应变与不变关系，不能代替源码/实际接口证据。导出前运行 [执行量门禁与预期关系检查](references/workload-delivery.md#执行量门禁与预期关系检查)，只针对影响目标的量和关系，不补无关参数表。可用环境下构造输入，核对 dtype/shape/stride、索引及状态；不能执行时如实记录未验证层级。

最终给出统一目录和入口、保留/跳过用例及理由、重跑命令、假设/未知字段、接入缺口和验证结果。明确区分静态/schema、输入构造、算子正确性和 NPU/性能实测；没有实测不填性能基线。

## 默认交付完成条件

- 推导说明能够说明目标边界、公式、关键约束、证据、用例选择与假设。
- 结构化结果与 workload 可逐条对应，可用脚本确定性重建。
- 输入生成器表达目标接口要求的数据关系；未知字段不被偷偷补成常量。
- 可执行 workload 只包含已具体化且满足接口约束的用例。待定符号留在结构化分析和跳过清单中，不伪装成可运行 case。
- definition/reference 已存在时核对接入兼容性；不存在时明确交付是 workload 与输入准备，给出所需接线，不宣称完整 benchmark 已运行。
